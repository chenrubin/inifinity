/* finds a path from start that goes over all Chess board squares,
   each square only once  
   Return value: SUCCESS for success and FAIL for fail*/
int HorsesTour(size_t start, size_t *result_path);
